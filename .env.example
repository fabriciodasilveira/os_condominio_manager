SECRET_KEY=change-me-in-production
ACCESS_TOKEN_EXPIRE_MINUTES=1440
DB_USER=condominio
DB_PASSWORD=condominio
DB_HOST=localhost
DB_PORT=5432
DB_NAME=condominio_os
# Porta externa usada no docker-compose para acesso ao Postgres (ex.: DBeaver)
DB_EXTERNAL_PORT=5433
# Opcional: sobrescreve os campos DB_* acima.
# DATABASE_URL=postgresql+psycopg://condominio:condominio@localhost:5432/condominio_os
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_CLAIMS_SUB=mailto:admin@condominio.local
